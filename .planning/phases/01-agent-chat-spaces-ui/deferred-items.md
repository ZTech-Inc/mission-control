# Deferred Items

- 2026-03-30: `pnpm typecheck` fails on pre-existing unrelated error in `src/lib/__tests__/db-seed-auth-pass.test.ts` (`DEFAULT_SEED_AUTH_PASS` not exported from `src/lib/db`). Not modified in plan `01-02`; deferred per scope boundary.
