---
phase: 04-agent-profile-enrichment
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/lib/__tests__/org-scanner-profile-rescan.test.ts
  - src/lib/__tests__/agent-profile-parser.test.ts
  - src/lib/agent-profile-parser.ts
  - src/lib/org-scanner.ts
  - src/app/api/agents/route.ts
  - src/app/api/agents/[id]/route.ts
  - src/components/panels/agent-detail-tabs.tsx
  - .planning/phases/04-agent-profile-enrichment/04-UAT.md
  - .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md
  - .planning/phases/04-agent-profile-enrichment/04-VALIDATION.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [PROF-01, PROF-02, PROF-03]

must_haves:
  truths:
    - "A forced org scan repopulates enriched profile fields for representative filesystem agents instead of leaving most fields null or unparsed"
    - "The discrete agent columns for skills, protocol_stack, kpis, deliverables, dependencies, preferred_runtime, and openclaw_id are populated through the real scanner path on both insert and rescan"
    - "Automated regression coverage fails if the force-rescan path drops enriched profile data again"
    - "Phase 04 verification artifacts no longer report an open rescan-profile gap"
  artifacts:
    - path: "src/lib/__tests__/org-scanner-profile-rescan.test.ts"
      provides: "Regression coverage for disk -> force scan -> SQLite profile persistence"
      min_lines: 60
    - path: "src/lib/agent-profile-parser.ts"
      provides: "Parser logic updated only as needed to handle the diagnosed failing markdown patterns"
      contains: "parseAgentProfile"
    - path: "src/lib/org-scanner.ts"
      provides: "Real scan/upsert path that persists enriched profile fields on forced rescans"
      contains: "getOrgSnapshot"
    - path: ".planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md"
      provides: "Updated verification result for the closed forced-rescan gap"
      contains: "status:"
    - path: ".planning/phases/04-agent-profile-enrichment/04-UAT.md"
      provides: "Updated UAT evidence for the formerly failing Test 2 rescan path"
      contains: "Force Org Scan Persists Enriched Profile Fields"
  key_links:
    - from: "src/lib/__tests__/org-scanner-profile-rescan.test.ts"
      to: "src/lib/org-scanner.ts"
      via: "getOrgSnapshot({ force: true }) against a temp AGENTS_DIR and SQLite DB"
      pattern: "getOrgSnapshot\\(\\{\\s*force:\\s*true"
    - from: "src/lib/org-scanner.ts"
      to: "src/lib/agent-profile-parser.ts"
      via: "parseAgentProfile during filesystem sync"
      pattern: "parseAgentProfile"
    - from: ".planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md"
      to: ".planning/phases/04-agent-profile-enrichment/04-UAT.md"
      via: "matching passed evidence for forced rescan population"
      pattern: "Force Org Scan Persists Enriched Profile Fields"
---

<objective>
Close the single recorded Phase 04 verification gap: forced org rescans currently leave many enriched profile fields null/unparsed, causing the Profile tab to show parse-failure badges instead of populated metadata.

Purpose: Preserve the completed Phase 04 work from 04-01 and 04-02, diagnose the real rescan failure in the live scanner path, apply the narrowest durable fix, add regression coverage for that path, and refresh verification/UAT artifacts so Phase 04 can be considered verification-clean.
Output: One new regression test for force rescans, minimal code changes to fix the root cause, and updated Phase 04 verification/UAT/state records.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-profile-enrichment/04-CONTEXT.md
@.planning/phases/04-agent-profile-enrichment/04-RESEARCH.md
@.planning/phases/04-agent-profile-enrichment/04-UAT.md
@.planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md
@.planning/phases/04-agent-profile-enrichment/04-01-SUMMARY.md
@.planning/phases/04-agent-profile-enrichment/04-02-SUMMARY.md
@src/lib/config.ts
@src/lib/db.ts
@src/lib/org-scanner.ts
@src/lib/agent-profile-parser.ts
@src/components/panels/agent-detail-tabs.tsx

<interfaces>
From `src/lib/org-scanner.ts`:
```typescript
export function getOrgSnapshot(options?: { force?: boolean; workspaceId?: number }): OrgSnapshot

function ensureFilesystemAgent(params: {
  workspaceId: number
  name: string
  role: string
  soulContent: string | null
  contentHash: string
  workspacePath: string
  config: Record<string, unknown>
  openclaw_id: string
  protocol_stack: string
  kpis: string
  deliverables: string
  dependencies: string
  preferred_runtime: string | null
  skills: string
}): number
```

From `src/lib/agent-profile-parser.ts`:
```typescript
export interface ParsedAgentProfile extends ParsedAgentMetadata {
  protocol_stack: string[]
  deliverables: string[]
  dependencies: string[]
  preferred_runtime: string | undefined
  openclaw_id: string
}

export function parseAgentProfile(
  agentDirName: string,
  agentMd: string,
  identityMd: string
): ParsedAgentProfile
```

From `src/lib/config.ts`:
```typescript
export const config = {
  agentsDir: process.env.AGENTS_DIR || process.env.MISSION_CONTROL_AGENTS_DIR || '',
  dataDir: resolvedDataDir,
  dbPath: resolvedDbPath,
}
```

Locked decisions still in force from `04-CONTEXT.md`:
- D-05 and D-06: parse failures still surface as `could not parse` badges when a scanned source exists but a field remains unparsed
- D-07 and D-08: filesystem wins for profile fields on rescan, while manual lead-role protection remains intact
- D-01 through D-04: keep the existing Profile tab structure and capabilities-first layout; this gap plan fixes data correctness, not a UI redesign
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the forced-rescan regression with scanner-level automated coverage</name>
  <files>src/lib/__tests__/org-scanner-profile-rescan.test.ts, src/lib/__tests__/agent-profile-parser.test.ts</files>
  <read_first>
    src/lib/org-scanner.ts
    src/lib/agent-profile-parser.ts
    src/lib/config.ts
    src/lib/db.ts
    src/lib/__tests__/agent-profile-parser.test.ts
    .planning/phases/04-agent-profile-enrichment/04-UAT.md
    .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md
  </read_first>
  <behavior>
    - A temp filesystem workspace containing representative AGENT.md and IDENTITY.md fixtures can be force-scanned through `getOrgSnapshot({ force: true })`
    - The test asserts the persisted `agents` row contains non-empty `skills`, `protocol_stack`, `kpis`, `deliverables`, `dependencies`, `preferred_runtime`, and `openclaw_id`
    - The same agent can be rescanned a second time and still keeps the enriched profile columns populated
    - The failing path is exercised through the real scanner and SQLite persistence layer, not by unit-testing `parseAgentProfile()` in isolation
  </behavior>
  <action>
Create a dedicated regression test for the exact gap reported in `04-UAT.md` Test 2. Do not touch product code yet except for any test-only seams required to isolate env and database state.

1. Add `src/lib/__tests__/org-scanner-profile-rescan.test.ts` that:
   - Creates a temporary `AGENTS_DIR` tree matching the real org-scanner expectations (`<root>/<department>/<team>/<agent>/AGENT.md` and `IDENTITY.md`)
   - Uses representative markdown that mirrors the section/table formats already seen in `src/lib/__tests__/agent-profile-parser.test.ts` and that can plausibly reproduce the UAT symptom where many fields land null/unparsed
   - Forces the scanner through the real entry point: set temp `AGENTS_DIR`, `MISSION_CONTROL_DATA_DIR`, `MISSION_CONTROL_DB_PATH`, and `MISSION_CONTROL_TEST_MODE=1`, reset modules, call `getOrgSnapshot({ force: true, workspaceId: 1 })`, then query SQLite with `getDatabase()`
   - Asserts the row for the scanned agent persists populated JSON-array columns plus a non-null runtime and derived `openclaw_id`
   - Re-runs the force scan after invalidating the snapshot cache and asserts the enriched columns remain populated on update, not only on first insert

2. Expand `src/lib/__tests__/agent-profile-parser.test.ts` only if the diagnosis shows a currently untested markdown pattern is the root cause. Keep this focused on parser grammar; the new scanner-level test is the required regression guard.

3. Make the new test fail first to prove the gap is real. Record the failure mode in the test name or inline comment so the later fix is traceable to the UAT gap.
  </action>
  <verify>
    <automated>cd /home/rahmanwolied/Wolied/z/mission-control && set +e; pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts; status=$?; set -e; test "$status" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__tests__/org-scanner-profile-rescan.test.ts` exists and exercises `getOrgSnapshot({ force: true })`
    - The new test writes a temp agent workspace to disk instead of stubbing `parseAgentProfile()`
    - The new test queries the persisted `agents` row and checks `skills`, `protocol_stack`, `kpis`, `deliverables`, `dependencies`, `preferred_runtime`, and `openclaw_id`
    - The task ends with the regression reproduced as a failing automated test before the implementation fix
    - `src/lib/__tests__/agent-profile-parser.test.ts` only changes if a missing parser grammar case is part of the diagnosis
  </acceptance_criteria>
  <done>The exact forced-rescan gap from `04-UAT.md` is pinned by an automated scanner-level regression test that currently fails on the unmodified codebase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the diagnosed force-rescan population bug without reopening Phase 04 scope</name>
  <files>src/lib/agent-profile-parser.ts, src/lib/org-scanner.ts, src/app/api/agents/route.ts, src/app/api/agents/[id]/route.ts, src/components/panels/agent-detail-tabs.tsx</files>
  <read_first>
    src/lib/agent-profile-parser.ts
    src/lib/org-scanner.ts
    src/app/api/agents/route.ts
    src/app/api/agents/[id]/route.ts
    src/components/panels/agent-detail-tabs.tsx
    src/lib/__tests__/org-scanner-profile-rescan.test.ts
    .planning/phases/04-agent-profile-enrichment/04-02-SUMMARY.md
  </read_first>
  <behavior>
    - The previously failing force-rescan regression now passes through the real scanner path
    - Agents returned from the API expose populated profile arrays/strings after a force scan, not raw JSON strings or nulls for fields that exist in source files
    - The Profile tab continues to render populated values when data exists and only shows `could not parse` when a scanned source exists but the field truly remains unparsed per D-05/D-06
  </behavior>
  <action>
Use the failing regression from Task 1 to identify the actual root cause, then implement the narrowest durable fix. Preserve the completed work from 04-01 and 04-02; do not redesign the Profile tab or rewrite the migration story.

Implementation rules:
1. Start at the real failure point surfaced by the new test.
   - If the parser misses real markdown formats, extend `parseAgentProfile()` just enough to cover those formats.
   - If the scanner/upsert path drops or overwrites fields during rescan, fix `src/lib/org-scanner.ts` there instead of broad parser changes.
   - If the data is persisted correctly but rendered as empty because of deserialization or badge logic, make the minimal API/UI correction in the listed files.

2. Preserve locked decisions:
   - Keep the existing Profile tab and capabilities-first layout from D-01 through D-04 intact.
   - Keep parse-failure badge semantics from D-05/D-06 intact; do not suppress the badge globally just to make the UAT look green.
   - Keep filesystem-wins rescan behavior from D-07 and manual lead protection from D-08 unchanged.

3. Treat 04-01 and 04-02 as background dependencies:
   - Reuse the existing parser/scanner pipeline.
   - Do not reopen migration 051 or rework unrelated profile UI sections.
   - Touch `src/app/api/agents/route.ts`, `src/app/api/agents/[id]/route.ts`, or `src/components/panels/agent-detail-tabs.tsx` only if the regression proves the persistence fix alone is insufficient.

4. Once the root cause is fixed, make both the new scanner-level regression and the existing parser tests pass.
  </action>
  <verify>
    <automated>cd /home/rahmanwolied/Wolied/z/mission-control && pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts src/lib/__tests__/agent-profile-parser.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts src/lib/__tests__/agent-profile-parser.test.ts` exits 0
    - The force-rescan regression proves populated enriched profile columns on both initial scan and rescan
    - Product code changes stay limited to the minimal files required by the diagnosed root cause
    - No change reopens migration design, profile layout structure, or manual-role protection behavior from prior plans
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>The recorded Phase 04 rescan bug is fixed in the real code path, the new regression stays green, and the existing parser coverage remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run Phase 04 verification evidence and close the recorded gap artifacts</name>
  <files>.planning/phases/04-agent-profile-enrichment/04-UAT.md, .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md, .planning/phases/04-agent-profile-enrichment/04-VALIDATION.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/04-agent-profile-enrichment/04-UAT.md
    .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md
    .planning/phases/04-agent-profile-enrichment/04-VALIDATION.md
    .planning/STATE.md
    src/lib/__tests__/org-scanner-profile-rescan.test.ts
  </read_first>
  <action>
Replace the stale evidence-only gap records with evidence from the fixed codebase.

1. Re-run the Phase 04 checks that directly speak to the gap:
   - `pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts src/lib/__tests__/agent-profile-parser.test.ts`
   - `pnpm typecheck`

2. Update `.planning/phases/04-agent-profile-enrichment/04-UAT.md`:
   - Change Test 2 (`Force Org Scan Persists Enriched Profile Fields`) from `issue` to `pass` only if the fixed regression coverage is green
   - Replace the old user-reported issue text with concrete evidence from the rerun commands
   - Clear the open gap block if no failure remains

3. Update `.planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md`:
   - Replace `status: gaps_found` with a passing state only if the rerun evidence is genuinely green
   - Update the score and the failed truth to reflect that the forced-rescan path is now verified
   - Remove the stale contradiction language once the new evidence supersedes it

4. Update `.planning/phases/04-agent-profile-enrichment/04-VALIDATION.md` so the new regression file appears in the Phase 04 validation map and the document is no longer missing Wave 0 coverage for this path.

5. Update `.planning/STATE.md` to remove the now-stale Phase 04 gap note and replace it with an accurate statement that Phase 04 is complete with the rescan verification gap closed, if and only if the evidence now supports that claim.
  </action>
  <verify>
    <automated>cd /home/rahmanwolied/Wolied/z/mission-control && pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts src/lib/__tests__/agent-profile-parser.test.ts && pnpm typecheck && rg -n "Force Org Scan Persists Enriched Profile Fields|status: pass|status: passed|gaps_found|wave_0_complete|rescan verification gap closed" .planning/phases/04-agent-profile-enrichment/04-UAT.md .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md .planning/phases/04-agent-profile-enrichment/04-VALIDATION.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 04 UAT record no longer reports Test 2 as an open issue
    - The Phase 04 verification report no longer reports the forced-rescan truth as failed
    - The validation document references the new regression coverage file
    - `STATE.md` matches the updated verification reality instead of the earlier inconsistency
    - No documentation claim is upgraded unless backed by the rerun commands from this task
  </acceptance_criteria>
  <done>Phase 04’s UAT, verification, validation, and state artifacts all reflect the closed forced-rescan gap and the evidence used to close it.</done>
</task>

</tasks>

<verification>
- `cd /home/rahmanwolied/Wolied/z/mission-control && pnpm test -- --run src/lib/__tests__/org-scanner-profile-rescan.test.ts src/lib/__tests__/agent-profile-parser.test.ts`
- `cd /home/rahmanwolied/Wolied/z/mission-control && pnpm typecheck`
- `cd /home/rahmanwolied/Wolied/z/mission-control && rg -n "Force Org Scan Persists Enriched Profile Fields" .planning/phases/04-agent-profile-enrichment/04-UAT.md .planning/phases/04-agent-profile-enrichment/04-VERIFICATION.md`
</verification>

<success_criteria>
- The exact Phase 04 forced-rescan gap from `04-UAT.md` is reproduced, fixed, and guarded by an automated regression
- The real scan path repopulates enriched profile fields on rescan without undoing prior Phase 04 decisions
- Phase 04 verification/UAT artifacts are updated from fresh evidence and no longer claim an open rescan-profile blocker
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-profile-enrichment/04-03-SUMMARY.md`
</output>
