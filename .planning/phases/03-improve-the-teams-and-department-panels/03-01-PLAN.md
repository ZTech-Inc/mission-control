---
phase: 03-improve-the-teams-and-department-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/index.ts
  - src/lib/org-scanner.ts
  - src/app/api/departments/[id]/lead/route.ts
  - src/components/panels/departments-panel.tsx
  - src/lib/use-org-data.ts
autonomous: true
requirements: [D-19, D-20, D-21]

must_haves:
  truths:
    - "All UI labels say 'Department Manager' instead of 'Department Lead'"
    - "Store action is named setDepartmentManager and calls the correct API"
    - "Org-scanner skips MANAGER/ directory when iterating teams"
    - "Org-scanner reads MANAGER/ directory contents as the department manager agent"
    - "Creation buttons are not blocked by isReadOnly in filesystem-backed mode"
  artifacts:
    - path: "src/store/index.ts"
      provides: "Renamed setDepartmentManager action"
      contains: "setDepartmentManager"
    - path: "src/lib/org-scanner.ts"
      provides: "MANAGER/ skip + manager agent extraction"
      contains: "MANAGER"
    - path: "src/components/panels/departments-panel.tsx"
      provides: "Department Manager UI labels"
      contains: "Department Manager"
    - path: "src/lib/use-org-data.ts"
      provides: "canCreate flag for creation bypass"
      contains: "canCreate"
  key_links:
    - from: "src/store/index.ts"
      to: "/api/departments/[id]/lead"
      via: "setDepartmentManager fetch call"
      pattern: "setDepartmentManager.*fetch"
    - from: "src/lib/org-scanner.ts"
      to: "MANAGER directory"
      via: "reserved directory skip"
      pattern: "RESERVED_DEPARTMENT_SUBDIRS|MANAGER"
---

<objective>
Rename "Department Lead" to "Department Manager" across the codebase (UI labels, store actions, API references), update org-scanner to handle the MANAGER/ directory convention, and add a `canCreate` flag so creation buttons work in filesystem-backed mode.

Purpose: Establishes the terminology and data-layer conventions that all subsequent plans depend on.
Output: Renamed store action, updated org-scanner, UI label changes, creation bypass flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-improve-the-teams-and-department-panels/03-CONTEXT.md
@.planning/phases/03-improve-the-teams-and-department-panels/03-RESEARCH.md
@.planning/phases/03-improve-the-teams-and-department-panels/03-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs -->

From src/store/index.ts:
```typescript
export interface Department {
  id: number;
  name: string;
  description?: string;
  color?: string;
  manager_agent_id?: number | null; // DB column already named this
  // ...
}

// Current action to rename:
setDepartmentLead: (deptId: number, agentId: number | null) => Promise<boolean>
// Line 1258-1275: implementation fetches /api/departments/${deptId}/lead
```

From src/lib/org-scanner.ts:
```typescript
const IGNORED_DIRECTORIES = new Set(['.git', '.github', '.vscode', '.idea', 'node_modules', '__pycache__', '.DS_Store'])
// Line 97-101: safeDirectories filters with IGNORED_DIRECTORIES
// Line 77-80: stableNumber(key) generates stable IDs from paths
```

From src/lib/use-org-data.ts:
```typescript
// Line 100: isReadOnly: orgSource === 'filesystem'
```

From src/components/panels/departments-panel.tsx:
```typescript
// Line 83: const setDepartmentLead = useMissionControl((s) => s.setDepartmentLead)
// Line 202: <h3>Department Lead</h3>
// Line 211: await setDepartmentLead(dept.id, ...)
// Line 534: subtitle="Assign a department lead to enable chat"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Department Lead to Department Manager across store, API, and UI</name>
  <files>
    src/store/index.ts,
    src/app/api/departments/[id]/lead/route.ts,
    src/components/panels/departments-panel.tsx
  </files>
  <read_first>
    src/store/index.ts,
    src/app/api/departments/[id]/lead/route.ts,
    src/components/panels/departments-panel.tsx
  </read_first>
  <action>
Per D-19 and D-20: Full rename of "Department Lead" to "Department Manager" in code and UI.

**src/store/index.ts:**
1. Rename the store interface method from `setDepartmentLead` to `setDepartmentManager` in the `MissionControlStore` interface (around line 643).
2. Rename the implementation from `setDepartmentLead` to `setDepartmentManager` (around line 1258). The fetch URL stays as `/api/departments/${deptId}/lead` for now (API route file stays in same path — renaming the route file is cosmetic and optional).
3. Update the error messages from "Failed to set department lead" to "Failed to set department manager".
4. Do NOT rename the `Department` interface field `manager_agent_id` — it is already correctly named (per Pitfall 6 in RESEARCH.md).

**src/components/panels/departments-panel.tsx:**
1. Line 83: Change `const setDepartmentLead = useMissionControl((s) => s.setDepartmentLead)` to `const setDepartmentManager = useMissionControl((s) => s.setDepartmentManager)`.
2. Line 202: Change `Department Lead` heading text to `Department Manager`.
3. Line 211: Change `await setDepartmentLead(dept.id, ...)` to `await setDepartmentManager(dept.id, ...)`.
4. Line 534: Change subtitle from `"Assign a department lead to enable chat"` to `"Assign a department manager to enable chat"`.
5. Search for any other occurrences of "Department Lead" or "department lead" in the file and rename to "Department Manager" / "department manager".

**Search all other files** for references to `setDepartmentLead` and update them. Use `grep -rn setDepartmentLead src/` to find all call sites.
  </action>
  <verify>
    <automated>grep -rn "setDepartmentLead\|Department Lead\|department lead" src/ | grep -v node_modules | grep -v ".planning" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "setDepartmentLead" src/store/index.ts` returns 0 matches
    - `grep -rn "setDepartmentManager" src/store/index.ts` returns at least 2 matches (interface + implementation)
    - `grep -rn "Department Lead" src/components/panels/departments-panel.tsx` returns 0 matches
    - `grep -rn "Department Manager" src/components/panels/departments-panel.tsx` returns at least 1 match
    - `grep -rn "setDepartmentManager" src/components/panels/departments-panel.tsx` returns at least 1 match
    - `pnpm typecheck` passes with no errors
  </acceptance_criteria>
  <done>All "Department Lead" references are renamed to "Department Manager" in UI labels. Store action is setDepartmentManager. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update org-scanner for MANAGER/ convention and add canCreate flag</name>
  <files>
    src/lib/org-scanner.ts,
    src/lib/use-org-data.ts
  </files>
  <read_first>
    src/lib/org-scanner.ts,
    src/lib/use-org-data.ts,
    src/lib/config.ts
  </read_first>
  <action>
Per D-21: Department Managers live in `MANAGER/` directory inside each department.

**src/lib/org-scanner.ts:**
1. Add a `RESERVED_DEPARTMENT_SUBDIRS` constant (or add to existing IGNORED_DIRECTORIES if simpler): `const RESERVED_DEPARTMENT_SUBDIRS = new Set(['MANAGER', 'docs'])`.
2. In the `scanFilesystemOrg` function (or `applyFilesystemOrgPersistence`), where team subdirectories are iterated under each department directory, filter out entries in `RESERVED_DEPARTMENT_SUBDIRS`. The current code at line ~100 uses `safeDirectories(dir)` which only filters `IGNORED_DIRECTORIES`. Add a second filter step when iterating department subdirectories for teams: `.filter(name => !RESERVED_DEPARTMENT_SUBDIRS.has(name))`.
3. After filtering teams, check if a `MANAGER/` subdirectory exists inside the department directory. If it does, scan its contents as an agent (look for IDENTITY.md, AGENT.md files same as regular agent scanning). Set the department's `manager_agent_id` to the discovered agent's ID.
4. When writing the manager agent to DB, use `source = 'filesystem'` (since it was scanned, not manually created). The manager agent should NOT be assigned to any team.

**src/lib/use-org-data.ts:**
1. Find the `isReadOnly` derivation (line ~100): `isReadOnly: orgSource === 'filesystem'`.
2. Add a new field `canCreate: true` — always `true` when `agentsDir` (from config) is a non-empty string. This allows creation buttons to work even when `isReadOnly` is true.
3. The return object should include both `isReadOnly` and `canCreate`. Existing consumers of `isReadOnly` are unaffected.
  </action>
  <verify>
    <automated>grep -n "RESERVED_DEPARTMENT_SUBDIRS\|MANAGER" src/lib/org-scanner.ts | head -5 && grep -n "canCreate" src/lib/use-org-data.ts | head -3 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RESERVED_DEPARTMENT_SUBDIRS" src/lib/org-scanner.ts` returns at least 1 match
    - `grep "'MANAGER'" src/lib/org-scanner.ts` returns at least 1 match
    - `grep "canCreate" src/lib/use-org-data.ts` returns at least 1 match
    - `pnpm typecheck` passes
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Org-scanner skips MANAGER/ when iterating teams, reads MANAGER/ contents as the department manager agent, and use-org-data exposes canCreate flag.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` passes
- `grep -rn "setDepartmentLead" src/` returns 0 matches (outside test stubs)
- `grep -rn "Department Lead" src/components/` returns 0 matches
- `grep -n "MANAGER" src/lib/org-scanner.ts` shows reserved directory handling
- `grep -n "canCreate" src/lib/use-org-data.ts` shows creation flag
</verification>

<success_criteria>
All "Department Lead" references renamed to "Department Manager" in code and UI. Org-scanner handles MANAGER/ as reserved department subdirectory. canCreate flag available for creation button enablement. TypeScript compiles and build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-improve-the-teams-and-department-panels/03-01-SUMMARY.md`
</output>
