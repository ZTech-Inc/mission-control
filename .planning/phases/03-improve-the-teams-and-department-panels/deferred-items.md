# Deferred Items

- 2026-03-30: `pnpm typecheck` fails on pre-existing Next route typing in `src/app/api/departments/[id]/docs/route.ts` (`/api/departments/[id]/docs` validator mismatch). Out of scope for plan `03-02` task files.
