# Deferred Items

## 2026-03-30 - Plan 03-01

- Out-of-scope typecheck failure in `.next/dev/types/validator.ts` referencing route typing mismatch for `src/app/api/teams/[id]/docs/route.ts`.
- Out-of-scope typecheck failure in `.next/dev/types/validator.ts` for missing `"/api/agents/create"` route type membership.
- These errors pre-existed Task 2 changes and are unrelated to `src/lib/org-scanner.ts` and `src/lib/use-org-data.ts`.
