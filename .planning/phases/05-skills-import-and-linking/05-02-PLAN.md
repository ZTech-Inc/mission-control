---
phase: 05-skills-import-and-linking
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/panels/skill-content-viewer.tsx
  - src/components/panels/skills-panel.tsx
  - src/components/panels/agent-detail-tabs.tsx
  - src/components/panels/__tests__/agent-detail-tabs.test.tsx
  - tests/skills-import-linking.spec.ts
autonomous: false
requirements: [SKIL-02]

must_haves:
  truths:
    - "Profile-tab skill chips link to the matching catalog entry when the same agent owns an exact normalized match."
    - "If there is no same-agent match, the profile chip links only when exactly one global exact normalized match exists."
    - "Ambiguous or missing matches remain plain non-clickable chips."
    - "Clicking a linked profile skill opens the existing read-only skill-detail surface instead of a new editor flow."
    - "Linked and unlinked skill chips remain visually consistent in the Profile tab."
  artifacts:
    - "src/components/panels/agent-detail-tabs.tsx :: same-agent-first link resolution and clickable profile skill chips :: exports ProfileTab"
    - "src/components/panels/skill-content-viewer.tsx :: reusable read-only SKILL.md viewer used by both panels :: exports SkillContentViewer"
    - "src/components/panels/__tests__/agent-detail-tabs.test.tsx :: component tests for linked, fallback, and ambiguous skill chips :: at least 60 lines"
    - "tests/skills-import-linking.spec.ts :: operator-facing regression for scan plus profile click-through :: contains skills import linking"
  key_links:
    - "src/components/panels/agent-detail-tabs.tsx -> /api/skills :: catalog lookup and content fetch :: pattern /api/skills"
    - "src/components/panels/agent-detail-tabs.tsx -> src/components/panels/skill-content-viewer.tsx :: shared read-only viewer :: pattern SkillContentViewer"
    - "src/components/panels/skills-panel.tsx -> src/components/panels/skill-content-viewer.tsx :: reused detail surface :: pattern SkillContentViewer"
---

<objective>
Add deterministic profile-to-catalog skill linking and reuse the existing skill detail surface from the Profile tab.

Purpose: Fulfill SKIL-02 without introducing a second skill viewer or ambiguous linking rules, while preserving the Phase 4 chip-based profile presentation.
Output: Clickable profile skill chips, reusable read-only skill viewer, regression coverage, and a blocking manual verification checkpoint for the real operator flow.
</objective>

<execution_context>
@/Users/rahmanwolied/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rahmanwolied/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-skills-import-and-linking/05-CONTEXT.md
@.planning/phases/05-skills-import-and-linking/05-RESEARCH.md
@.planning/phases/05-skills-import-and-linking/05-VALIDATION.md
@.planning/phases/05-skills-import-and-linking/05-01-PLAN.md
@.planning/phases/04-agent-profile-enrichment/04-02-SUMMARY.md
@AGENTS.md

<interfaces>
From src/store/index.ts:
```typescript
export interface Agent {
  id: number
  name: string
  skills?: string[]
  workspace_path?: string
  source?: string
  content_hash?: string
}
```

From src/components/panels/agent-detail-tabs.tsx:
```typescript
function ProfileChip({ label }: { label: string })
function ProfileField({ label, values, canParse }: { label: string; values?: string[]; canParse: boolean })
export function ProfileTab({ agent }: { agent: Agent })
```

From src/components/panels/skills-panel.tsx:
```typescript
interface SkillSummary {
  id: string
  name: string
  source: string
  path: string
}

interface SkillContentResponse {
  source: string
  name: string
  skillPath: string
  skillDocPath: string
  content: string
}
```

From Plan 01 expected outputs:
```typescript
export function buildOrgAgentSkillSource(agentName: string): string
// /api/skills groups and mode=content both support org-agent:* sources
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock same-agent-first skill linking with component and e2e tests</name>
  <files>src/components/panels/__tests__/agent-detail-tabs.test.tsx, tests/skills-import-linking.spec.ts</files>
  <read_first>
    src/components/panels/agent-detail-tabs.tsx
    src/components/panels/skills-panel.tsx
    .planning/phases/05-skills-import-and-linking/05-CONTEXT.md
    .planning/phases/05-skills-import-and-linking/05-VALIDATION.md
  </read_first>
  <behavior>
    - Same-agent exact normalized match links first per D-07.
    - Unique global exact normalized fallback links when no same-agent match exists per D-08.
    - Multiple global exact matches leave the chip inert per D-09.
    - Clicking a linked chip opens the shared read-only viewer per D-10.
  </behavior>
  <action>
    Add failing tests before implementation.

    In `src/components/panels/__tests__/agent-detail-tabs.test.tsx`, write component tests that mount `ProfileTab` with a fake agent and a mocked `/api/skills` response set:
    - a same-agent source match at `org-agent:atlas-coordinator` for `Retrospective`;
    - a unique global fallback match for `PromptPlanning`;
    - two conflicting global matches for `Research`;
    - no match for `DeepWork`.

    Assert exact DOM behavior:
    - `Retrospective` renders as a button or link and triggers the viewer fetch.
    - `PromptPlanning` renders as a button or link only when it is the sole global exact match.
    - `Research` renders as static text with no click handler when multiple matches exist.
    - `DeepWork` renders as a static chip.

    In `tests/skills-import-linking.spec.ts`, create a Playwright test named `skills import linking` that:
    - seeds or scans a filesystem-backed agent with a `skills/` directory;
    - opens that agent's Profile tab;
    - clicks a linked skill chip;
    - verifies the viewer shows the expected `source` label and `SKILL.md` content text.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && test -f src/components/panels/__tests__/agent-detail-tabs.test.tsx && test -f tests/skills-import-linking.spec.ts && rg -n "Retrospective|PromptPlanning|Research|DeepWork" src/components/panels/__tests__/agent-detail-tabs.test.tsx && rg -n "skills import linking" tests/skills-import-linking.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/panels/__tests__/agent-detail-tabs.test.tsx` succeeds.
    - `test -f tests/skills-import-linking.spec.ts` succeeds.
    - `rg -n "Retrospective|PromptPlanning|Research|DeepWork" src/components/panels/__tests__/agent-detail-tabs.test.tsx` returns matches.
    - `rg -n "skills import linking" tests/skills-import-linking.spec.ts` returns a match.
  </acceptance_criteria>
  <done>
    The repo contains failing tests that define deterministic profile skill linking and the operator-facing click-through behavior before UI code changes land.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the skill viewer and implement deterministic profile chip linking</name>
  <files>src/components/panels/skill-content-viewer.tsx, src/components/panels/skills-panel.tsx, src/components/panels/agent-detail-tabs.tsx</files>
  <read_first>
    src/components/panels/agent-detail-tabs.tsx
    src/components/panels/skills-panel.tsx
    src/app/api/skills/route.ts
    src/lib/agent-skills-importer.ts
    src/store/index.ts
  </read_first>
  <action>
    Implement the profile link flow without introducing a second skill-detail UX.

    Create `src/components/panels/skill-content-viewer.tsx` as a reusable read-only viewer component extracted from the current drawer content in `skills-panel.tsx`. It must render:
    - selected skill name;
    - source label;
    - read-only markdown content;
    - security status/issues if present;
    - loading and error states.

    Update `src/components/panels/skills-panel.tsx` to use `SkillContentViewer` for its existing detail surface while preserving current editable controls for non-`org-agent:*` sources.

    Update `src/components/panels/agent-detail-tabs.tsx` so the Skills section:
    - fetches catalog rows from `/api/skills` when needed;
    - normalizes skill labels for exact matching;
    - resolves same-agent matches first using `buildOrgAgentSkillSource(agent.name)` per D-07;
    - falls back only to a single exact normalized global match per D-08;
    - leaves ambiguous or missing matches as inert chips per D-09;
    - opens `SkillContentViewer` with the selected catalog row when a linked chip is clicked per D-10;
    - keeps linked and plain chips visually aligned with the existing `ProfileChip` styling per D-11.

    Do not add linking anywhere except the Profile tab skills list per D-12.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && pnpm test -- --run src/components/panels/__tests__/agent-detail-tabs.test.tsx && pnpm test:e2e --grep "skills import linking"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/panels/skill-content-viewer.tsx` succeeds.
    - `rg -n "SkillContentViewer" src/components/panels/skills-panel.tsx src/components/panels/agent-detail-tabs.tsx` returns matches.
    - `rg -n "buildOrgAgentSkillSource|/api/skills|normalize" src/components/panels/agent-detail-tabs.tsx` returns matches.
    - `pnpm test -- --run src/components/panels/__tests__/agent-detail-tabs.test.tsx` exits 0.
    - `pnpm test:e2e --grep "skills import linking"` exits 0.
  </acceptance_criteria>
  <done>
    Profile-tab skill chips deterministically resolve to the correct catalog entry, the viewer is shared with the Skills panel, and ambiguous chips remain non-clickable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the real org-scan and profile-link flow in the browser</name>
  <files>src/components/panels/agent-detail-tabs.tsx, src/components/panels/skills-panel.tsx, src/components/panels/skill-content-viewer.tsx</files>
  <read_first>
    .planning/phases/05-skills-import-and-linking/05-VALIDATION.md
    src/components/panels/agent-detail-tabs.tsx
    src/components/panels/skills-panel.tsx
  </read_first>
  <action>
    Perform the blocking operator verification on the real dashboard after Task 2 automation passes. Use a filesystem-backed agent with imported skills, verify the Profile-tab chip states, and confirm the shared read-only viewer opens the expected imported SKILL.md content without exposing edit controls for `org-agent:*`.
  </action>
  <what_built>
    Agent-local skills import into the catalog and deterministic profile-tab linking into the shared read-only skill viewer.
  </what_built>
  <how-to-verify>
    1. Run a forced filesystem org scan against a local agent that has `skills/` entries.
    2. Open that agent in the dashboard and switch to the Profile tab.
    3. Confirm a linked skill chip is visibly interactive while an unmatched or ambiguous chip remains plain.
    4. Click a linked skill chip and confirm the viewer shows the expected `org-agent:<agent-name-slug>` source label and the correct `SKILL.md` body.
    5. Open the main Skills panel and confirm the same imported source group is listed there as read-only.
  </how-to-verify>
  <resume-signal>
    Type `approved` to close the plan, or describe the exact mismatch you observed.
  </resume-signal>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && pnpm test -- --run src/components/panels/__tests__/agent-detail-tabs.test.tsx && pnpm test:e2e --grep "skills import linking"</automated>
  </verify>
  <acceptance_criteria>
    - The operator can identify at least one linked skill chip and one inert chip in the Profile tab.
    - The viewer opened from a linked chip shows the exact imported source namespace `org-agent:<agent-name-slug>`.
    - The same imported source appears in the Skills panel without save/delete affordances for that row.
  </acceptance_criteria>
  <done>
    The real browser flow confirms imported skills can be discovered from the agent profile without opening the wrong skill or exposing edit paths for filesystem-owned content.
  </done>
</task>

</tasks>

<verification>
Run the component test before each UI commit and keep the Playwright regression green for the end-to-end operator path. Do not close this plan without the blocking human verification checkpoint.
</verification>

<success_criteria>
SKIL-02 is satisfied when Profile-tab skills resolve same-agent exact matches first, use only a unique global exact fallback, keep ambiguous chips inert, and open the shared read-only skill viewer with the correct imported catalog entry.
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-import-and-linking/05-02-SUMMARY.md`
</output>
