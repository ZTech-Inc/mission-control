---
phase: 05-skills-import-and-linking
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/components/panels/skills-panel.tsx
  - src/components/panels/__tests__/skills-panel-org-agent.test.tsx
autonomous: true
requirements: [SKIL-01]
gap_closure: true

must_haves:
  truths:
    - "The /skills catalog shows imported org-agent sources in a dedicated installed-catalog section separate from global and project skill roots."
    - "Imported org-agent entries are identified with explicit source language, not only an emerald border treatment."
    - "Imported org-agent rows remain read-only and keep the existing view-only catalog flow."
    - "Existing non-org-agent catalog roots still appear in the installed catalog without losing their current behaviors."
  artifacts:
    - "src/components/panels/skills-panel.tsx :: explicit installed-catalog section headings and imported-source badge copy :: contains Imported Agent Skills and Global and Project Skill Sources"
    - "src/components/panels/__tests__/skills-panel-org-agent.test.tsx :: regression coverage for sectioning and explicit imported-source labeling :: contains Imported Agent Skills and Imported from agent workspace"
  key_links:
    - "src/components/panels/skills-panel.tsx -> skillGroups from store :: org-agent grouping split via source.startsWith('org-agent:') :: pattern startsWith('org-agent:')"
    - "src/components/panels/skills-panel.tsx -> getSourceLabel() :: imported cards still show agent-specific labels such as Atlas Coordinator skills :: pattern getSourceLabel(group.source)"
    - "src/components/panels/__tests__/skills-panel-org-agent.test.tsx -> SkillsPanel :: verifies explicit section labels instead of color-only differentiation :: pattern Imported Agent Skills"
---

<objective>
Close the Phase 05 UAT gap in the skills catalog by making imported org-agent skills immediately distinguishable from global and project roots.

Purpose: Fix the discoverability failure recorded in `05-UAT.md` without changing import behavior, edit permissions, or introducing the larger scrolling/navigation work deferred to Phase 08.
Output: A narrow catalog UI follow-up that adds explicit imported-vs-global sectioning and label copy, plus regression coverage for that distinction.
</objective>

<execution_context>
@/Users/rahmanwolied/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rahmanwolied/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-skills-import-and-linking/05-CONTEXT.md
@.planning/phases/05-skills-import-and-linking/05-UAT.md
@.planning/phases/05-skills-import-and-linking/05-VALIDATION.md
@.planning/phases/05-skills-import-and-linking/05-01-SUMMARY.md
@.planning/phases/05-skills-import-and-linking/05-02-PLAN.md
@AGENTS.md

<interfaces>
From src/components/panels/skills-panel.tsx:
```typescript
interface SkillGroup {
  source: string
  path: string
  skills: SkillSummary[]
}

export function getSourceLabel(source: string): string
export function SkillsPanel()
```

From src/components/panels/skill-content-viewer.tsx:
```typescript
export function getSkillSourceLabel(source: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the catalog distinction gap with explicit org-agent sectioning tests</name>
  <files>src/components/panels/__tests__/skills-panel-org-agent.test.tsx</files>
  <read_first>
    src/components/panels/__tests__/skills-panel-org-agent.test.tsx
    src/components/panels/skills-panel.tsx
    src/components/panels/skill-content-viewer.tsx
    .planning/phases/05-skills-import-and-linking/05-UAT.md
    .planning/phases/05-skills-import-and-linking/05-CONTEXT.md
  </read_first>
  <behavior>
    - Imported `org-agent:*` sources render under a dedicated installed-catalog section instead of relying on border color alone.
    - Imported cards include explicit copy that says they came from agent workspaces.
    - Global and project sources remain outside the imported section and retain their existing editable/catalog placement.
    - Imported entries stay read-only.
  </behavior>
  <action>
    Extend `src/components/panels/__tests__/skills-panel-org-agent.test.tsx` with failing regression coverage for the exact UAT complaint.

    Add a store fixture that contains at least:
    - one imported source group with `source: 'org-agent:atlas-coordinator'`
    - one editable source group with `source: 'user-agents'`
    - one editable source group with a representative project source such as `source: 'project-main'`

    Add assertions for these exact visible strings in the rendered installed catalog:
    - section heading `Imported Agent Skills`
    - section helper copy `Read-only skills imported from agent workspaces during org scans.`
    - section heading `Global and Project Skill Sources`
    - imported-card badge text `Imported from agent workspace`
    - agent-readable source label `Atlas Coordinator skills`
    - editable-source label `~/.agents/skills (global)`
    - project-source label `project skills`

    Add a negative assertion that the imported section does not rely solely on the existing emerald card outline by requiring one of the explicit text labels above to be present before opening the drawer.

    Keep the existing read-only assertion path for `org-agent:*` entries and the writable assertion paths for both `user-agents` and the representative `project-*` source. Use that project-family assertion to prove Task 2's non-org preservation claim covers more than a single global-root fixture.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && test -f src/components/panels/__tests__/skills-panel-org-agent.test.tsx && rg -n "Imported Agent Skills|Read-only skills imported from agent workspaces during org scans\\.|Global and Project Skill Sources|Imported from agent workspace|Atlas Coordinator skills|~/.agents/skills \\(global\\)|project skills|project-main" src/components/panels/__tests__/skills-panel-org-agent.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/panels/__tests__/skills-panel-org-agent.test.tsx` succeeds.
    - `rg -n "Imported Agent Skills" src/components/panels/__tests__/skills-panel-org-agent.test.tsx` returns a match.
    - `rg -n "Read-only skills imported from agent workspaces during org scans\\." src/components/panels/__tests__/skills-panel-org-agent.test.tsx` returns a match.
    - `rg -n "Global and Project Skill Sources" src/components/panels/__tests__/skills-panel-org-agent.test.tsx` returns a match.
    - `rg -n "Imported from agent workspace" src/components/panels/__tests__/skills-panel-org-agent.test.tsx` returns a match.
    - `rg -n "~/.agents/skills \\(global\\)|project skills|project-main" src/components/panels/__tests__/skills-panel-org-agent.test.tsx` returns matches proving both global and project-family non-org sources are covered.
  </acceptance_criteria>
  <done>
    The test suite describes the exact catalog text and section boundaries needed to close the UAT gap, and it now proves that both global and representative project-family sources remain outside the imported org-agent section before any UI changes are implemented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit imported-vs-global sectioning in the installed skills catalog</name>
  <files>src/components/panels/skills-panel.tsx, src/components/panels/__tests__/skills-panel-org-agent.test.tsx</files>
  <read_first>
    src/components/panels/skills-panel.tsx
    src/components/panels/__tests__/skills-panel-org-agent.test.tsx
    src/components/panels/skill-content-viewer.tsx
    .planning/phases/05-skills-import-and-linking/05-UAT.md
    .planning/phases/05-skills-import-and-linking/05-01-SUMMARY.md
  </read_first>
  <action>
    Update `src/components/panels/skills-panel.tsx` so the installed catalog shows imported org-agent skills as a clearly separate source family while preserving all existing flows.

    Implement these concrete UI states:
    - Split `(skillGroups || [])` into `orgAgentGroups = groups.filter((group) => group.source.startsWith('org-agent:'))` and `defaultGroups = groups.filter((group) => !group.source.startsWith('org-agent:'))`.
    - Render an installed-catalog section heading `Imported Agent Skills` above `orgAgentGroups`.
    - Render the helper copy `Read-only skills imported from agent workspaces during org scans.` directly under that heading.
    - Render a second section heading `Global and Project Skill Sources` above `defaultGroups`.
    - For every `orgAgentGroups` card, render the inline badge copy `Imported from agent workspace` in addition to the existing source label from `getSourceLabel(group.source)`.
    - Keep the existing count, path, filter-click behavior, drawer `view` action, and read-only footer for imported rows.
    - Keep existing rendering and editability for `user-agents`, `user-codex`, `project-*`, `openclaw`, and `workspace-*` sources.

    Do not add pagination, tabs, sticky side panels, collapsed accordions, virtualized lists, or any other large-layout/navigation changes from Phase 08. This plan is limited to explicit catalog distinction text and grouping inside the current installed skills view.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && pnpm test -- --run src/components/panels/__tests__/skills-panel-org-agent.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Imported Agent Skills|Global and Project Skill Sources|Read-only skills imported from agent workspaces during org scans\\." src/components/panels/skills-panel.tsx` returns matches.
    - `rg -n "Imported from agent workspace" src/components/panels/skills-panel.tsx` returns at least one match.
    - `rg -n "startsWith\\('org-agent:'\\)" src/components/panels/skills-panel.tsx` returns at least one match.
    - `rg -n "getSourceLabel\\(group.source\\)" src/components/panels/skills-panel.tsx` returns at least one match.
    - `pnpm test -- --run src/components/panels/__tests__/skills-panel-org-agent.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    The installed skills catalog makes imported org-agent sources obvious through explicit section headings and imported-source copy, while imported entries remain read-only and all non-org-agent catalog flows stay intact.
  </done>
</task>

</tasks>

<verification>
Run the targeted Skills panel test after each task. Before closing the plan, confirm the installed skills catalog contains explicit imported-source copy and dedicated section headings, with no new navigation or long-scroll restructuring work.
</verification>

<success_criteria>
The Phase 05 UAT gap is closed when an operator can open `/skills` and immediately tell which entries came from imported org-agent workspaces because those entries appear in a dedicated `Imported Agent Skills` section with explicit imported-source text, while the existing global/project sources remain in their own section and imported rows stay read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-import-and-linking/05-03-SUMMARY.md`
</output>
