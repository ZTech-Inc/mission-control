---
phase: 05-skills-import-and-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-skills-importer.ts
  - src/lib/__tests__/agent-skills-importer.test.ts
  - src/lib/__tests__/skills-route-org-agent.test.ts
  - src/lib/org-scanner.ts
  - src/app/api/skills/route.ts
  - src/components/panels/skills-panel.tsx
  - src/components/panels/__tests__/skills-panel-org-agent.test.tsx
autonomous: true
requirements: [SKIL-01]

must_haves:
  truths:
    - "A forced org scan imports every agent-local SKILL.md under workspace_path/skills into the existing skills catalog."
    - "Imported skill rows are stored under org-agent:<agent-name-slug> and do not collide with existing global sources."
    - "Re-importing one agent updates that agent's imported rows in place and deletes only rows missing from that same agent source."
    - "Imported org-agent rows remain visible and readable through the existing skills catalog API and skills panel."
    - "Global sync behavior does not claim ownership of org-agent rows."
  artifacts:
    - "src/lib/agent-skills-importer.ts :: dedicated org-agent skill import and cleanup logic :: exports syncOrgAgentSkills and buildOrgAgentSkillSource"
    - "src/lib/org-scanner.ts :: org-scan lifecycle hook triggers agent skill import after profile sync :: contains syncOrgAgentSkills usage"
    - "src/app/api/skills/route.ts :: DB-backed org-agent content lookup and arbitrary-source grouping :: exports GET"
    - "src/components/panels/skills-panel.tsx :: visible org-agent source groups and read-only treatment in the catalog :: contains org-agent handling"
  key_links:
    - "src/lib/org-scanner.ts -> src/lib/agent-skills-importer.ts :: post-profile import hook :: pattern syncOrgAgentSkills"
    - "src/lib/agent-skills-importer.ts -> skills table :: INSERT/UPDATE/DELETE scoped by source and name :: pattern DELETE FROM skills WHERE source = \\?"
    - "src/app/api/skills/route.ts -> skills.path :: DB-backed SKILL.md content lookup :: pattern SELECT .* FROM skills WHERE source = \\? AND name = \\?"
---

<objective>
Create the source-aware import pipeline for agent-local skills and make imported rows first-class catalog entries.

Purpose: Fulfill SKIL-01 by importing `skills/SKILL.md` files during org scans without breaking the existing global skill registry or the read-only catalog UX.
Output: Dedicated org-agent importer, scanner hook, catalog API updates, and visible org-agent groups in the Skills panel.
</objective>

<execution_context>
@/Users/rahmanwolied/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rahmanwolied/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-skills-import-and-linking/05-CONTEXT.md
@.planning/phases/05-skills-import-and-linking/05-RESEARCH.md
@.planning/phases/05-skills-import-and-linking/05-VALIDATION.md
@.planning/phases/04-agent-profile-enrichment/04-02-SUMMARY.md
@AGENTS.md

<interfaces>
From src/lib/org-scanner.ts:
```typescript
function ensureFilesystemAgent(params: {
  workspaceId: number
  name: string
  role: string
  soulContent: string | null
  contentHash: string
  workspacePath: string
  config: Record<string, unknown>
  openclaw_id: string
  protocol_stack: string
  kpis: string
  deliverables: string
  dependencies: string
  preferred_runtime: string | null
  skills: string
}): number

function scanFilesystemOrg(rootPath: string, workspaceId: number): OrgSnapshot
```

From src/lib/skill-sync.ts:
```typescript
export async function syncSkillsFromDisk(): Promise<{ ok: boolean; message: string }>
```

From src/app/api/skills/route.ts:
```typescript
interface SkillSummary {
  id: string
  name: string
  source: string
  path: string
  description?: string
}

export async function GET(request: NextRequest)
```

From src/components/panels/skills-panel.tsx:
```typescript
function getSourceLabel(source: string): string
const [selectedSkill, setSelectedSkill] = useState<SkillSummary | null>(null)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write importer and catalog API regression tests for org-agent sources</name>
  <files>src/lib/__tests__/agent-skills-importer.test.ts, src/lib/__tests__/skills-route-org-agent.test.ts</files>
  <read_first>
    src/lib/skill-sync.ts
    src/app/api/skills/route.ts
    src/lib/org-scanner.ts
    .planning/phases/05-skills-import-and-linking/05-RESEARCH.md
    .planning/phases/05-skills-import-and-linking/05-VALIDATION.md
  </read_first>
  <behavior>
    - `syncOrgAgentSkills` uses `org-agent:<agent-name-slug>` per D-01 and D-02.
    - Re-import updates changed skill rows and deletes only missing rows for that same source per D-06.
    - Generic `syncSkillsFromDisk()` ownership never includes `org-agent:*` sources per D-05.
    - `GET /api/skills?mode=content&source=org-agent:...&name=...` resolves from the stored DB path instead of `getSkillRoots()` per D-05.
    - Grouped skills responses include `org-agent:*` sources so imported rows are not hidden in the catalog.
  </behavior>
  <action>
    Create failing tests before implementation.

    In `src/lib/__tests__/agent-skills-importer.test.ts`, add fixtures that create temp `skills/<name>/SKILL.md` directories for one agent workspace and assert all of the following with exact source strings:
    - `buildOrgAgentSkillSource('Atlas Coordinator')` returns `org-agent:atlas-coordinator` per D-01 and D-02.
    - First import inserts rows keyed by `(source='org-agent:atlas-coordinator', name='...')`.
    - Second import with modified SKILL.md content updates `content_hash`, `description`, and `updated_at`.
    - Removing `skills/Retrospective/SKILL.md` deletes only `source='org-agent:atlas-coordinator' AND name='Retrospective'` while leaving rows for `org-agent:beacon-lead` untouched.
    - Empty or missing `skills/` directory removes only that agent source when re-imported.

    In `src/lib/__tests__/skills-route-org-agent.test.ts`, mock the DB and filesystem so the GET handler proves:
    - `mode=content` returns 200 for a row whose `source` starts with `org-agent:` and whose `path` points to a real SKILL directory.
    - `mode=content` returns 404 when the DB row exists but `SKILL.md` is absent.
    - The default grouped response contains a group object with `source: 'org-agent:atlas-coordinator'`.
    - The default flat response still includes per-source rows, not a hidden drop caused by unknown-source filtering.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && test -f src/lib/__tests__/agent-skills-importer.test.ts && test -f src/lib/__tests__/skills-route-org-agent.test.ts && rg -n "org-agent:atlas-coordinator|buildOrgAgentSkillSource|syncOrgAgentSkills" src/lib/__tests__/agent-skills-importer.test.ts && rg -n "mode=content|org-agent:atlas-coordinator|groups" src/lib/__tests__/skills-route-org-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/__tests__/agent-skills-importer.test.ts` succeeds.
    - `test -f src/lib/__tests__/skills-route-org-agent.test.ts` succeeds.
    - `rg -n "org-agent:atlas-coordinator|buildOrgAgentSkillSource|syncOrgAgentSkills" src/lib/__tests__/agent-skills-importer.test.ts` returns matches.
    - `rg -n "mode=content|org-agent:atlas-coordinator|groups" src/lib/__tests__/skills-route-org-agent.test.ts` returns matches.
  </acceptance_criteria>
  <done>
    The repo contains failing regression tests that lock D-01 through D-06 into executable checks for importer behavior and catalog API behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedicated org-agent import and DB-backed catalog access</name>
  <files>src/lib/agent-skills-importer.ts, src/lib/org-scanner.ts, src/app/api/skills/route.ts</files>
  <read_first>
    src/lib/agent-skills-importer.ts
    src/lib/skill-sync.ts
    src/lib/org-scanner.ts
    src/app/api/skills/route.ts
    src/lib/migrations.ts
    .planning/phases/05-skills-import-and-linking/05-CONTEXT.md
  </read_first>
  <action>
    Implement the dedicated importer and wire it into the scan lifecycle.

    Create `src/lib/agent-skills-importer.ts` with:
    - `export function buildOrgAgentSkillSource(agentName: string): string` that normalizes the persisted Phase 4 agent identity into `org-agent:<agent-name-slug>` per D-01 and D-02. Reuse the same lowercase slugging behavior already used for agent identity normalization; do not derive from department or team names.
    - `export async function syncOrgAgentSkills(params: { agentId: number; agentName: string; workspacePath: string | null }): Promise<{ source: string; created: number; updated: number; deleted: number; total: number }>` that scans `<workspacePath>/skills/*/SKILL.md`, extracts the first non-heading paragraph as `description`, hashes content, upserts rows into `skills(name, source, path, description, content_hash, installed_at, updated_at)`, and deletes only rows for that exact source missing from disk per D-04, D-05, and D-06.
    - Explicit no-op behavior when `workspacePath` is null, empty, or unreadable.

    Update `src/lib/org-scanner.ts` so `scanFilesystemOrg()` records the persisted agents that were just synced and, after the profile-sync transaction completes, calls `syncOrgAgentSkills()` for each filesystem agent using `workspace_path`. Keep this import path separate from `syncSkillsFromDisk()` per D-05.

    Update `src/app/api/skills/route.ts` so:
    - `mode=content` first looks up `SELECT source, name, path FROM skills WHERE source = ? AND name = ?` and reads `${row.path}/SKILL.md` for `org-agent:*` rows instead of rejecting unknown sources.
    - the grouped response includes arbitrary DB sources, not only `workspace-*`.
    - the flat `skills` array preserves imported org-agent rows so same-name skills from different sources remain addressable by source.
    - write endpoints remain root-backed for editable sources only; do not add write support for `org-agent:*`.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && pnpm test -- --run src/lib/__tests__/agent-skills-importer.test.ts src/lib/__tests__/skills-route-org-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export function buildOrgAgentSkillSource|export async function syncOrgAgentSkills" src/lib/agent-skills-importer.ts` returns matches.
    - `rg -n "syncOrgAgentSkills" src/lib/org-scanner.ts` returns at least one match.
    - `rg -n "SELECT source, name, path FROM skills WHERE source = \\? AND name = \\?" src/app/api/skills/route.ts` returns a match.
    - `rg -n "startsWith\\('org-agent:'\\)|org-agent:" src/app/api/skills/route.ts` returns at least one match.
    - `pnpm test -- --run src/lib/__tests__/agent-skills-importer.test.ts src/lib/__tests__/skills-route-org-agent.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Forced org scans have a dedicated org-agent import step, imported rows survive generic skill sync assumptions, and the catalog API can open imported SKILL.md content by source and name.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose org-agent groups in the Skills panel and keep them read-only</name>
  <files>src/components/panels/skills-panel.tsx, src/components/panels/__tests__/skills-panel-org-agent.test.tsx</files>
  <read_first>
    src/components/panels/skills-panel.tsx
    src/app/api/skills/route.ts
    .planning/phases/05-skills-import-and-linking/05-RESEARCH.md
  </read_first>
  <action>
    Update the existing Skills panel to treat imported org-agent rows as visible catalog entries without offering unsafe write actions.

    In `src/components/panels/skills-panel.tsx`:
    - extend `getSourceLabel()` so `org-agent:atlas-coordinator` renders as an operator-readable label such as `Atlas Coordinator skills` instead of raw namespace text.
    - include `org-agent:*` groups in the installed-source cards and list filtering logic so imported rows are not hidden by the current whitelist.
    - when `selectedSkill.source.startsWith('org-agent:')`, keep the existing read-only detail surface but hide or disable save/delete/create paths for that selected row. Do not add edit support, per D-10 and the out-of-scope editor rule.
    - preserve all existing behavior for editable sources such as `user-agents`, `user-codex`, `project-*`, `openclaw`, and `workspace-*`.

    Add `src/components/panels/__tests__/skills-panel-org-agent.test.tsx` with automated coverage for the read-only branch:
    - render the panel with an `org-agent:atlas-coordinator` selected skill and assert save/delete/create affordances are absent or disabled;
    - render with an editable source and assert those affordances still appear;
    - assert the formatted source label is operator-readable rather than raw namespace text.
  </action>
  <verify>
    <automated>cd /Users/rahmanwolied/Documents/Work/Ztech/mission-control && pnpm test -- --run src/lib/__tests__/skills-route-org-agent.test.ts src/components/panels/__tests__/skills-panel-org-agent.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "org-agent:" src/components/panels/skills-panel.tsx` returns at least one match.
    - `rg -n "getSourceLabel\\(|startsWith\\('org-agent:'\\)" src/components/panels/skills-panel.tsx` returns matches.
    - `test -f src/components/panels/__tests__/skills-panel-org-agent.test.tsx` succeeds.
    - `rg -n "saveSkill|deleteSkill" src/components/panels/skills-panel.tsx` still returns matches for editable sources, with conditional handling for `org-agent:*`.
  </acceptance_criteria>
  <done>
    Imported agent skills appear in the existing Skills panel with readable source labels and a read-only detail flow that does not attempt disk writes back into agent workspaces.
  </done>
</task>

</tasks>

<verification>
Run the importer/API regression suite after each commit in this plan. Before marking the wave complete, confirm a local forced org scan imports agent-local rows without changing generic `syncSkillsFromDisk()` ownership semantics.
</verification>

<success_criteria>
SKIL-01 is satisfied when a forced org scan imports agent-local `SKILL.md` files into the `skills` table under `org-agent:<slug>`, imported rows remain visible and openable in the catalog, and re-import only deletes rows missing from that same agent source.
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-import-and-linking/05-01-SUMMARY.md`
</output>
