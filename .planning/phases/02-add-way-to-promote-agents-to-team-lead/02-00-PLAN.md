---
phase: 02-add-way-to-promote-agents-to-team-lead
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/team-assignment-route.test.ts
  - src/lib/__tests__/department-lead-route.test.ts
  - src/lib/__tests__/org-scanner-source-priority.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-14
  - D-17

must_haves:
  truths:
    - "Test stubs exist for team assignment PATCH route before implementation"
    - "Test stubs exist for department lead PUT route before implementation"
    - "Test stubs exist for org-scanner source priority guard before implementation"
  artifacts:
    - path: "src/lib/__tests__/team-assignment-route.test.ts"
      provides: "Test scaffold for PATCH /api/teams/:id/assignments"
      contains: "describe"
    - path: "src/lib/__tests__/department-lead-route.test.ts"
      provides: "Test scaffold for PUT /api/departments/:id/lead"
      contains: "describe"
    - path: "src/lib/__tests__/org-scanner-source-priority.test.ts"
      provides: "Test scaffold for scanner upsert source priority"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all three test files identified in the validation strategy. These stubs define expected behaviors before implementation begins in Wave 1.

Purpose: Nyquist compliance requires test scaffolds to exist before implementation tasks reference them. Wave 1 plans use these test files in their verify commands.
Output: Three test stub files with describe blocks and pending test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-add-way-to-promote-agents-to-team-lead/02-CONTEXT.md
@.planning/phases/02-add-way-to-promote-agents-to-team-lead/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for API routes and org-scanner source priority</name>
  <files>src/lib/__tests__/team-assignment-route.test.ts, src/lib/__tests__/department-lead-route.test.ts, src/lib/__tests__/org-scanner-source-priority.test.ts</files>
  <read_first>
    - src/lib/__tests__ (check directory exists and see existing test file patterns)
    - src/lib/org-scanner.ts (lines 340-348 for upsertAssignment to understand what to test)
  </read_first>
  <action>
Create three test stub files. Each file should use `describe` and `it.todo()` (or `test.todo()`) to declare expected behaviors without implementing assertions yet. The implementation plans (02-01 and 02-02) will fill in assertions as they create the production code.

**1. `src/lib/__tests__/team-assignment-route.test.ts`:**

```typescript
import { describe, it } from 'vitest'

describe('PATCH /api/teams/:id/assignments', () => {
  it.todo('returns 401 when not authenticated')
  it.todo('returns 400 when team id is not a number')
  it.todo('returns 422 when agent is not a member of the team (per D-17)')
  it.todo('promotes agent to lead and returns 200')
  it.todo('demotes existing lead when promoting a new agent (per D-14)')
  it.todo('sets source to manual on promoted assignment')
  it.todo('allows demoting a lead back to member')
})
```

**2. `src/lib/__tests__/department-lead-route.test.ts`:**

```typescript
import { describe, it } from 'vitest'

describe('PUT /api/departments/:id/lead', () => {
  it.todo('returns 401 when not authenticated')
  it.todo('returns 400 when department id is not a number')
  it.todo('sets manager_agent_id on department and returns 200')
  it.todo('returns 404 when department does not exist')
  it.todo('allows unsetting lead by passing agent_id: null')
  it.todo('calls invalidateOrgSnapshot after update')
})
```

**3. `src/lib/__tests__/org-scanner-source-priority.test.ts`:**

```typescript
import { describe, it } from 'vitest'

describe('org-scanner source priority', () => {
  it.todo('filesystem upsert does not overwrite role when source is manual')
  it.todo('filesystem upsert does not overwrite source when existing source is manual')
  it.todo('filesystem upsert does update role when source is filesystem')
})

describe('getOrgSnapshot manager_agent_id propagation', () => {
  it.todo('departments in snapshot include manager_agent_id from database')
  it.todo('departments without manager_agent_id in DB have no manager_agent_id in snapshot')
})
```

These are `.todo()` stubs only. They will fail with "todo" status (not errors) when run, providing documentation of expected behavior.
  </action>
  <verify>
    <automated>pnpm test --run src/lib/__tests__/team-assignment-route.test.ts src/lib/__tests__/department-lead-route.test.ts src/lib/__tests__/org-scanner-source-priority.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Three test stub files exist in src/lib/__tests__/ with .todo() test cases covering team assignment PATCH, department lead PUT, and org-scanner source priority behaviors. `pnpm test --run` reports them as todo (not failing).</done>
</task>

</tasks>

<verification>
- All three test files exist in src/lib/__tests__/
- `pnpm test --run` passes (todo tests do not count as failures)
- Test file names match VALIDATION.md references exactly
</verification>

<success_criteria>
- src/lib/__tests__/team-assignment-route.test.ts exists with describe + todo tests
- src/lib/__tests__/department-lead-route.test.ts exists with describe + todo tests
- src/lib/__tests__/org-scanner-source-priority.test.ts exists with describe + todo tests
- No test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-add-way-to-promote-agents-to-team-lead/02-00-SUMMARY.md`
</output>
